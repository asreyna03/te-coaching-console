streamlit>=1.30
pandas>=2.0
altair>=5.0
# Persistent client storage (Supabase Postgres) — active when DATABASE_URL is set:
psycopg2-binary>=2.9
# Client plan PDF export:
fpdf2
# NOT deployed: the Google-Sheet food-DB refresh (sync.py) is a local-only CLI.
# Before running it locally:  pip install google-api-python-client google-auth
